<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mobile"
        tools:targetApi="31">
        <activity
            android:name=".view.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.SignupActivity"
            android:label="Sign Up"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        </activity>
        <activity android:name=".view.AddAbsenceActivity"
            android:label="Ajouter une Absence" />
        <activity
            android:name=".view.ManageAbsencesActivity"
            android:label="Gestion des Absences" />
        <activity
            android:name=".view.ManageUsersActivity"
            android:label="Gestion des Utilisateurs"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".view.StatisticsActivity" />
        <activity android:name=".view.ConsulterEnseignantAbsencesActivity" />



        <service android:name="com.google.firebase.messaging.FirebaseMessagingService"
            android:exported="false"/>

    </application>

</manifest>